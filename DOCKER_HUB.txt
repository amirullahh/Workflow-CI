Docker Hub Repository Information
=================================

Author: Amirullah
Project: House Prices ML Model - CI/CD Pipeline
Date: February 2026

Docker Hub Repository:
----------------------
URL: https://hub.docker.com/r/amirullahh/msml-model

Docker Image:
-------------
Image Name: amirullahh/msml-model:latest

Pull Command:
-------------
docker pull amirullahh/msml-model:latest

Run Command:
------------
docker run -p 5000:8080 amirullahh/msml-model:latest

Usage:
------
This Docker image contains the trained House Prices prediction model
built using MLflow's build-docker command. The image automatically runs
an MLflow model serving endpoint.

Make predictions:
curl -X POST http://localhost:5000/invocations \
  -H 'Content-Type: application/json' \
  -d '{"dataframe_split": {"columns": [...], "data": [[...]]}}'

Notes:
------
- Image is automatically built and pushed by GitHub Actions workflow
- Latest tag always points to the most recent training run
- Image includes MLflow model server and all dependencies
- Built using: mlflow models build-docker

GitHub Actions Configuration:
------------------------------
Required Secrets:
- DOCKERHUB_USERNAME: amirullahh
- DOCKERHUB_TOKEN: Your Docker Hub access token

Workflow File: .github/workflows/mlflow-ci.yml

Verification:
-------------
Docker Hub URL: https://hub.docker.com/r/amirullahh/msml-model
Status: ✓ Active
Latest Push: Auto-updated by GitHub Actions workflow
Image Size: ~2-3 GB (includes MLflow server + model + dependencies)
