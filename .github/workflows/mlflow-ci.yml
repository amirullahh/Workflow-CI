name: MLflow CI/CD Pipeline - House Prices

on:
  push:
    branches:
      - main
    paths:
      - 'MLProject/**'
      - '.github/workflows/**'
  pull_request:
    branches:
      - main
  workflow_dispatch:  # Manual trigger

env:
  PYTHON_VERSION: '3.12.7'
  MLFLOW_VERSION: '2.19.0'

jobs:
  train-model:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
    - name: 📥 Checkout Repository
      uses: actions/checkout@v4

    - name: 🐍 Set up Python ${{ env.PYTHON_VERSION }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: 📦 Install Dependencies
      run: |
        python -m pip install --upgrade pip wheel
        pip install setuptools==69.0.0
        pip install mlflow==${{ env.MLFLOW_VERSION }}
        pip install scikit-learn==1.4.0 pandas==2.2.0 numpy==1.26.3 scipy==1.12.0
        pip install matplotlib>=3.8.0 seaborn>=0.13.0 joblib>=1.3.0

    - name: 🔍 Verify Installation
      run: |
        echo "Python version:"
        python --version
        echo ""
        echo "Installed packages:"
        pip list | grep -E "(mlflow|scikit-learn|pandas|numpy)"

    - name: 🏗️ Run MLflow Project
      run: |
        cd MLProject
        echo "Starting MLflow training pipeline..."
        python modelling.py 200 15 42

    - name: 📊 Display Training Results
      run: |
        echo "=== Training Results ==="
        if [ -f "MLProject/artifacts/metrics_summary.json" ]; then
          cat MLProject/artifacts/metrics_summary.json
        else
          echo "Metrics summary not found"
        fi

    - name: 💾 Upload Model Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: trained-model-${{ github.run_number }}
        path: |
          MLProject/models/
          MLProject/artifacts/
          MLProject/mlruns/
        retention-days: 30
        compression-level: 6

    - name: 📝 Commit Artifacts to Repository
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"

        # Add model artifacts (exclude large mlruns folder)
        git add MLProject/models/*.pkl || true
        git add MLProject/artifacts/*.png || true
        git add MLProject/artifacts/*.json || true

        # Check if there are changes to commit
        if git diff --staged --quiet; then
          echo "No changes to commit"
        else
          git commit -m "🤖 [CI] Update model artifacts - Run #${{ github.run_number }}"
          git push || echo "Push failed (might be protected branch)"
        fi
      continue-on-error: true

    - name: 📋 Generate Training Report
      run: |
        cat << 'EOF' > $GITHUB_STEP_SUMMARY
        ## 🎯 MLflow Training Pipeline - Results

        **Workflow Run:** #${{ github.run_number }}
        **Commit:** ${{ github.sha }}
        **Branch:** ${{ github.ref_name }}

        ### 📊 Model Performance

        EOF

        if [ -f "MLProject/artifacts/metrics_summary.json" ]; then
          echo '```json' >> $GITHUB_STEP_SUMMARY
          cat MLProject/artifacts/metrics_summary.json >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
        fi

        cat << 'EOF' >> $GITHUB_STEP_SUMMARY

        ### ✅ Artifacts Generated

        - 🎯 Model file: `models/model.pkl`
        - 📈 Feature importance plot
        - 📊 Actual vs Predicted plot
        - 📄 Metrics summary JSON

        ### 📥 Download Artifacts

        Artifacts are available in the **Artifacts** section below for 30 days.

        ---
        🤖 *Generated by MLflow CI/CD Pipeline*
        EOF

  build-docker:
    needs: train-model
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
    - name: 📥 Checkout Repository
      uses: actions/checkout@v4

    - name: 📥 Download Model Artifacts
      uses: actions/download-artifact@v4
      with:
        name: trained-model-${{ github.run_number }}
        path: MLProject/

    - name: 🐍 Set up Python ${{ env.PYTHON_VERSION }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: 📦 Install MLflow
      run: |
        python -m pip install --upgrade pip wheel
        pip install setuptools==69.0.0
        pip install mlflow==${{ env.MLFLOW_VERSION }}

    - name: 🐳 Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: 🔑 Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
      continue-on-error: true

    - name: 🏗️ Build Docker Image with MLflow
      run: |
        cd MLProject

        # Check if mlruns exists
        if [ ! -d "mlruns" ]; then
          echo "❌ MLruns directory not found"
          exit 1
        fi
        echo "✓ MLruns directory found"

        # List mlruns structure for debugging
        echo "MLruns structure:"
        ls -la mlruns/

        # Find run with model artifacts (search in mlruns root)
        # MLflow can store runs at root level or inside experiment folders
        echo "Searching for model artifacts..."

        MODEL_PATH=""

        # Search for model directory in mlruns (any level)
        for dir in mlruns/*/artifacts/model mlruns/*/*/artifacts/model; do
          if [ -d "$dir" ] && [ -f "$dir/MLmodel" ]; then
            MODEL_PATH="$dir"
            echo "✓ Found model at: $MODEL_PATH"
            break
          fi
        done

        if [ -z "$MODEL_PATH" ]; then
          echo "❌ No model found in mlruns"
          echo "Searching all directories:"
          find mlruns -name "MLmodel" -type f 2>/dev/null || echo "No MLmodel files found"
          exit 1
        fi

        # List model contents
        echo "Model contents:"
        ls -la "$MODEL_PATH/"

        # Verify required files
        if [ ! -f "$MODEL_PATH/MLmodel" ]; then
          echo "❌ MLmodel file missing"
          exit 1
        fi
        echo "✓ MLmodel file verified"

        if [ ! -f "$MODEL_PATH/conda.yaml" ]; then
          echo "⚠️  Warning: conda.yaml missing (may cause build issues)"
        else
          echo "✓ conda.yaml verified"
        fi

        # Build Docker image
        echo "🐳 Building Docker image..."
        mlflow models build-docker \
          --model-uri "$MODEL_PATH" \
          --name "${{ secrets.DOCKERHUB_USERNAME }}/msml-model:latest"

        echo "✓ Docker image built successfully"
      continue-on-error: true
      env:
        DOCKER_BUILDKIT: 1

    - name: 🚀 Push Docker Image to Docker Hub
      run: |
        if docker images | grep -q "msml-model"; then
          echo "Pushing Docker image to Docker Hub..."
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/msml-model:latest || echo "Docker push failed"

          echo "### 🐳 Docker Image Published" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Image:** \`${{ secrets.DOCKERHUB_USERNAME }}/msml-model:latest\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Pull command:**" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
          echo "docker pull ${{ secrets.DOCKERHUB_USERNAME }}/msml-model:latest" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
        else
          echo "No Docker image found to push"
        fi
      continue-on-error: true

    - name: 🧹 Cleanup
      run: |
        docker system prune -f || true

  save-to-gdrive:
    needs: train-model
    runs-on: ubuntu-latest
    if: github.event_name == 'push'

    steps:
    - name: 📥 Download Model Artifacts
      uses: actions/download-artifact@v4
      with:
        name: trained-model-${{ github.run_number }}
        path: artifacts-backup/

    - name: 📦 Create Archive
      run: |
        cd artifacts-backup
        tar -czf ../model-artifacts-run-${{ github.run_number }}.tar.gz .
        cd ..
        ls -lh model-artifacts-run-${{ github.run_number }}.tar.gz

    - name: 💾 Upload Backup Artifact
      uses: actions/upload-artifact@v4
      with:
        name: model-backup-${{ github.run_number }}
        path: model-artifacts-run-${{ github.run_number }}.tar.gz
        retention-days: 90
